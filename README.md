# PythonBilledeBehandling

To run server

open Server.py

Set "HOST" to the computers local IP address and set "PORT" to a free port

In bash or terminal, navigate to working directory and run the server by running this line

```$ python3 server.py```
